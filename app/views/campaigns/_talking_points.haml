- target = campaign_target.target
%h2 Don't know what to say?
%h4 The clearest thing you can tell #{target.name} is:
.script
  %h4 "#{campaign_target.script}"
  = link_to "Hang Up", '#', class: 'hang-up btn large', data: {behavior: 'hangUpTrigger'}
